{
  "name": "matcha",
  "version": "1.0.0",
  "description": "",
  "main": "core/server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"node --loader ts-node/esm --inspect ./core/server.ts",
    "prod": "cross-env NODE_ENV=production build",
    "dev": "cross-env NODE_ENV=development build",
    "watch": "nodemon --watch dev --watch ./**/*.ts "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.8.4",
    "ws": "^8.8.1"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/node": "^18.7.18",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.0"
  }
}
