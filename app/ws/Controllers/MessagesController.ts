import { AuthInfo } from "../../auth/Auth";
import WSClient from "../WsClient/WSClient";

export default class MessagesController{
    static recirveChatMessage(data:any,client:WSClient,authInfo:AuthInfo){
        // const chat = data['']
    }
}